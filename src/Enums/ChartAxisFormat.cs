namespace RadarSoft.RadarCube.Enums
{
    /// <summary>
    ///     Enumerates all possible axis types (discrete or continuous).
    /// </summary>
    public enum ChartAxisFormat
    {
        /// <summary>
        ///     The discrete type (the axis descriptor is a hierarchy level).
        /// </summary>
        Discrete,

        /// <summary>
        ///     The continuous type (the axis descriptor is a measure).
        /// </summary>
        Continuous
    }
}