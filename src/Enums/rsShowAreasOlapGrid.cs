namespace RadarSoft.RadarCube.Enums
{
    /// <summary>
    ///     Specifies which areas (Data, Cube Structure Tree and Pivot) are displayed in the OLAP Grid
    /// </summary>
    public enum rsShowAreasOlapGrid
    {
        rsAll,
        rsPivot,
        rsDataOnly,
        rsTreeOnly
    }
}