namespace RadarSoft.RadarCube.ClientAgents
{
    /// <exclude />
    public enum GenericMenuActionType
    {
        Nothing,
        RefreshData,
        RefreshAll,
        ShowDialog,
        Separator,
        RedirectTo,
        Postback,
        CustomAction,
        Copy,
        ExecuteFunction
    }
}